# Task 10-4: Add Dashboard Automated Tests

[Back to task list](./tasks.md)

## Status History

| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-11-09 21:48:49 | Created | N/A | Proposed | Pending automated coverage for dashboard metrics | AI Agent |

## Description

Create automated tests validating the correctness of `/dashboard/api/health` calculations and ensuring the dashboard template renders without errors.

## Requirements

- Unit tests covering health score computation given mocked database responses.
- Integration test verifying endpoint returns expected JSON structure.
- Template smoke test ensuring Jinja renders with sample context.
- Tests should cover edge cases (no income, negative net income, zero budgets).

## Implementation Plan

1. Mock database queries using fixtures or in-memory SQLite.
2. Write unit tests for health score weighting logic.
3. Add integration test hitting Flask test client at `/dashboard/api/health`.
4. Add template render test verifying page loads.

## Test Plan

- Run `pytest` to ensure new tests pass and integrate with existing suite.
- Validate failure cases (e.g., missing data) surface descriptive errors.

## Verification

- [ ] Unit tests created
- [ ] Integration tests created
- [ ] Template smoke test created
- [ ] CI run updated to include new tests

## Files Modified

- _Pending implementation_ (expected: `tests/unit/test_dashboard.py`, `tests/integration/test_dashboard.py`)

