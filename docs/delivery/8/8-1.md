# Task 8-1: Create Savings Goals Schema

[Back to task list](./tasks.md)

## Status History

| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-11-09 21:48:49 | Created | N/A | Proposed | Backfilled documentation for existing migration | AI Agent |
| 2025-11-09 21:48:49 | Status Update | Proposed | InProgress | Verified schema implementation | AI Agent |
| 2025-11-09 21:48:49 | Status Update | InProgress | Done | Confirmed migration creates `savings_goals` table with constraints | AI Agent |

## Description

Introduce a database migration that creates the `savings_goals` table capturing goal metadata (target amounts, current progress, target date, category linkage, status).

## Requirements

- Table columns: `id`, `name`, `target_amount`, `current_amount`, `target_date`, `category_id`, `status`, `created_at`, `updated_at`.
- `status` constrained to (`active`, `completed`, `cancelled`).
- Foreign key to `categories.id`.
- Migration must be idempotent and print informative messages.

## Implementation Plan

1. Create `src/migrate_add_goals.py`.
2. Define SQL for table creation with checks/foreign key.
3. Ensure script handles existing table gracefully.
4. Provide CLI entry point to run migration.

## Test Plan

- Execute migration on fresh DB; table should appear.
- Re-run migration to ensure no errors (idempotent).
- Insert sample goal row and confirm constraints.

## Verification

- [x] Migration script exists (`src/migrate_add_goals.py`)
- [x] Table definition matches requirements
- [ ] Migration executed against current environment
- [ ] Sample insert validated manually

_Verification performed via code inspection on 2025-11-09._

## Files Modified

- `src/migrate_add_goals.py` (NEW)

