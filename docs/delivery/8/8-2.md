# Task 8-2: Implement Savings Goal Model

[Back to task list](./tasks.md)

## Status History

| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-11-09 21:48:49 | Created | N/A | Proposed | Documenting existing goal model implementation | AI Agent |
| 2025-11-09 21:48:49 | Status Update | Proposed | InProgress | Audited `src/models/goal.py` | AI Agent |
| 2025-11-09 21:48:49 | Status Update | InProgress | Done | Confirmed CRUD helpers align with schema | AI Agent |

## Description

Provide a Python model abstraction encapsulating CRUD operations for the `savings_goals` table, including helper methods to list goals and update progress.

## Requirements

- `get_all()` returns goals with joined category name.
- `create(...)` inserts new goal with timestamps.
- `update(goal_id, updates)` allows partial updates and maintains `updated_at`.
- `delete(goal_id)` removes goal by id.
- Input validation deferred to future API layer.

## Implementation Plan

1. Create `src/models/goal.py` with `Goal` class.
2. Implement read/write methods using SQLite connections.
3. Ensure connections are closed in all paths.
4. Provide docstrings for clarity.

## Test Plan

- Instantiate `Goal` with test DB and exercise CRUD in unit tests.
- Validate `get_all()` returns category names via join.
- Confirm `update` handles partial payloads and returns bool.

## Verification

- [x] `Goal` model implemented with CRUD methods
- [x] `get_all()` joins category name
- [ ] Automated tests executed for model

_Verification performed via code inspection on 2025-11-09._

## Files Modified

- `src/models/goal.py` (NEW)

