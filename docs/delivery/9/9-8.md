# Task 9-8: Integration Tests for Recurring Detection

[Back to task list](./tasks.md)

## Status History

| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-11-09 21:48:49 | Created | N/A | Proposed | Integration test coverage for recurring flow not yet implemented | AI Agent |

## Description

Author integration tests that exercise the complete recurring detection workflow: import transactions, detect patterns, persist recurring records, generate alerts, and surface them via API/UI endpoints. These tests guard against regressions in the cross-cutting flow spanning detector, manager, routes, and templates.

## Requirements

- Seed transactions spanning multiple frequencies and merchants.
- Execute scanner/manager logic to create recurring patterns.
- Hit `/recurring/api` endpoints to validate JSON payloads.
- Confirm alert generation for missing payments and amount changes.
- Cover auto-categorization integration once task 9-5 completes.

## Implementation Plan

1. Create integration suite `tests/integration/test_recurring.py`.
2. Use fixtures to seed transactions across accounts and frequencies.
3. Invoke detector/manager functions to simulate detection/save flow.
4. Assert API responses (`/recurring/api/all`, `/recurring/api/alerts`, `/recurring/api/stats`) match expectations.
5. Add CLI smoke test using `subprocess` to run `scan_recurring.py` with test database.

## Test Plan

- Scenario A: Monthly subscription detected, alert generated when overdue.
- Scenario B: Amount variance triggers change alert and UI reflects it.
- Scenario C: Recurring record created via CLI and exposed via API.
- Scenario D: Transactions without recurring patterns do not create records.
- Scenario E: Auto-categorization integration covered after task 9-5 completes.

## Verification

- [ ] Integration tests created
- [ ] Fixtures cover multiple recurring frequencies
- [ ] API endpoints validated via tests
- [ ] CLI smoke test added
- [ ] Test suite runs in CI

## Files Modified

- _Pending implementation_ (expected: `tests/integration/test_recurring.py`, fixtures)

