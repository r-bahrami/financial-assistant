# [7-5] Add budget vs actual reports
[Back to task list](./tasks.md)

## Status History
| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-10-20 00:15:00 | Created | N/A | Proposed | Task file created | Saeed |
| 2025-11-09 22:55:00 | Status Update | Proposed | InProgress | Implementing budget chart APIs and UI components | AI Agent |
| 2025-11-09 22:55:00 | Status Update | InProgress | Done | Budget vs actual charts added to budgets page with Chart.js visualizations | AI Agent |

## Requirements
- Bar chart: budget vs actual for all categories
- Line chart: budget performance over time
- Variance analysis (over/under for each category)
- Add to budgets page
- Interactive Chart.js visualizations

## Implementation Plan
- Extend `BudgetService` with helpers to compute per-category variance and monthly performance trends.
- Add REST endpoints in `budgets` blueprint serving chart-ready datasets.
- Update `budgets.html` to include Chart.js visualizations (bar + line) with responsive cards and empty-state handling.
- Refresh charts automatically after CRUD operations to keep visuals in sync.

## Test Plan
- Hit `/budgets/api/charts/category` and `/budgets/api/charts/trend` to confirm successful payload structure.
- Verify budgets page renders both charts, tooltips, and reacts to over/under budget status colours.
- Ensure empty datasets display helpful messaging rather than crashes.
- Add integration tests seeding budgets/transactions to validate new endpoints.

## Verification
- [x] Category comparison API returns labels/budgeted/actual/variance arrays
- [x] Trend API delivers six-month time series with aggregated totals
- [x] Budgets UI renders interactive Chart.js visuals with responsive styling
- [x] Charts update after budget changes and handle empty states gracefully
- [x] Integration coverage added (`tests/integration/test_budgets.py`)

## Files Modified
- `src/templates/budgets.html` - Add charts section
- `src/routes/budgets.py` - Chart data API endpoints
- `src/services/budget_service.py` - Add chart data methods
- `tests/integration/test_budgets.py` - New integration tests validating chart APIs
